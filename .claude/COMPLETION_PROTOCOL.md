# COMPLETION_PROTOCOL.md — Протокол завершения спринта

> Читай этот файл когда пользователь говорит: "заверши", "завершить", "финиш", "закончи", "done", "finish"
> Или использует команду `/fi`

---

## 🔄 Sprint Completion Checklist

**Выполняй последовательно:**

### 1. Сборка и проверка
```bash
npm run build
```
- [ ] Код компилируется без ошибок
- [ ] Функциональность протестирована

### 2. Обновление версий

**Найти и обновить версию везде:**
```bash
# Поиск текущей версии в проекте
grep -r "version.*2\." --include="*.json" --include="*.md" --include="*.ts"
```

Места где может быть версия:
- [ ] `package.json` — поле `version`
- [ ] `README.md` — badge версии
- [ ] `CHANGELOG.md` — заголовок новой версии
- [ ] `.claude/CLAUDE.md` — в заголовке
- [ ] `.claude/PROJECT_SNAPSHOT.md` — статус
- [ ] Код (если есть константы VERSION)

### 3. Обновить метафайлы

**Обязательно:**
- [ ] `.claude/BACKLOG.md` — отметить выполненные задачи `[x]`
- [ ] `.claude/PROJECT_SNAPSHOT.md` — обновить дату и прогресс
- [ ] `CHANGELOG.md` — добавить запись о изменениях

**При существенных изменениях:**
- [ ] `.claude/ARCHITECTURE.md` — если изменилась архитектура, модули, data flow
- [ ] `.claude/SECURITY.md` — если изменились аспекты безопасности

### 4. Экспорт диалогов
```bash
npm run dialog:export
```
- [ ] Диалоги экспортированы в `dialog/`
- [ ] HTML viewer обновлён в `html-viewer/`
- [ ] Публичные диалоги готовы к коммиту

### 5. Git commit
```bash
git add -A
git status  # Проверить что коммитится
git commit -m "$(cat <<'EOF'
type: Brief description

- Detail 1
- Detail 2

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

**Типы коммитов:**
- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — документация
- `refactor:` — рефакторинг
- `chore:` — обслуживание

### 6. Спросить про push

**ВАЖНО: Спросить пользователя:**
```
✅ Коммит создан!

Сделать git push?
```

Если да:
```bash
git push
```

---

## 🎯 Критерии завершения

Спринт завершён когда:

1. ✅ Код компилируется (`npm run build`)
2. ✅ Версии обновлены везде
3. ✅ Метафайлы актуальны
4. ✅ Диалоги экспортированы
5. ✅ Коммит создан
6. ✅ Push сделан (если пользователь подтвердил)

---

## 📝 Version Numbering

- **Patch (x.x.N):** Bug fixes, minor improvements
- **Minor (x.N.0):** New features
- **Major (N.0.0):** Breaking changes

---

## ⚠️ Напоминание

После команды "заверши":
```
📋 Запускаю Completion Protocol...
   1. npm run build
   2. Обновляю версии
   3. Обновляю метафайлы
   4. npm run dialog:export
   5. git commit
   6. Спрашиваю про push
```

---

*Протокол завершения спринта для claude-export*
