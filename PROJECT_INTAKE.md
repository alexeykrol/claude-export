# Project Intake Form

**Purpose:** Описание проекта claude-export для AI-агентов
**Status:** ✅ FILLED
**Last Updated:** 2025-12-05

---

## 🎯 Project Overview

### 1. Ключевая идея (Elevator Pitch)

CLI-утилита для экспорта диалогов Claude Code из `~/.claude/projects/` в читаемый Markdown формат с управлением видимостью через `.gitignore`.

---

### 2. Проблема (The Problem)

**Какую конкретную проблему решаем:**

Диалоги Claude Code хранятся в системной директории (`~/.claude/projects/`) в формате JSONL и:
- Недоступны для просмотра в проекте
- Не могут быть включены в Git историю
- Невозможно поделиться с командой
- Сложно искать и анализировать

**Почему существующие решения не работают:**

- Нет встроенного экспорта в Claude Code
- Ручное копирование JSONL файлов нечитаемо
- Нет механизма управления приватностью диалогов

---

### 3. Решение (The Solution)

**Как именно приложение решает проблему:**

1. **Экспорт** — конвертирует JSONL сессии в читаемый Markdown
2. **Локализация** — сохраняет диалоги в `.dialog/` папку проекта
3. **Приватность** — по умолчанию добавляет в `.gitignore`
4. **Автоматизация** — watch-режим для автоматического экспорта
5. **UI** — веб-интерфейс для управления диалогами
6. **Авторство** — автоматическая атрибуция из `git config`

**Уникальная ценность:**

- Диалоги становятся частью проекта
- Контроль видимости для команды
- Автоматическая генерация саммари
- Zero-config установка

---

### 4. Целевая аудитория (Target Audience)

**Для кого:**
- Разработчики, использующие Claude Code
- Преподаватели, демонстрирующие работу с AI
- Команды, практикующие AI-assisted разработку

**Характеристики:**
- Возраст: 20-50
- Профессия: Разработчики, преподаватели
- Технологическая грамотность: Высокая
- Готовность платить: Бесплатно (open source)

---

## 👥 User Personas

### Persona 1: Алексей, 35 лет

```
Имя: Алексей, 35 лет
Роль: Senior разработчик, тимлид
Локация: Москва, работает удаленно
```

**Цели:**
- Сохранять полезные диалоги с Claude Code для команды
- Делиться паттернами решения задач
- Иметь историю принятия решений в проекте

**Проблемы:**
- Диалоги теряются после закрытия сессии
- Команда не видит как он работает с AI
- Нет единого места для хранения best practices

**Поведение с технологиями:**
- Активно использует CLI инструменты
- Привык к npm/node экосистеме
- Ценит автоматизацию

---

### Persona 2: Мария, 42 года

```
Имя: Мария, 42 года
Роль: Преподаватель курса AI-assisted Development
Локация: Санкт-Петербург
```

**Цели:**
- Показывать студентам реальные диалоги с Claude Code
- Создавать учебные материалы на основе сессий
- Демонстрировать процесс работы с AI

**Проблемы:**
- Студенты не видят её диалоги
- Нужно вручную копировать примеры
- Нет контроля что публиковать, что скрыть

---

## 🗺️ User Flows

### Ключевой сценарий 1: Первая установка и экспорт

```
1. Пользователь клонирует claude-export в .claude-export/ папку проекта
2. Запускает install.sh
3. Видит сообщение об успешной инициализации
4. Запускает npm run dialog:ui
5. Открывает http://localhost:3333
6. Видит список своих диалогов с проектом
7. Переключает видимость нужных диалогов
8. Коммитит публичные диалоги в Git
```

---

### Ключевой сценарий 2: Автоматический экспорт

```
1. Пользователь запускает npm run dialog:watch
2. Watcher стартует в фоновом режиме
3. Пользователь работает с Claude Code
4. При завершении сессии диалог автоматически экспортируется
5. Через 30 секунд генерируется саммари
6. Диалог появляется в .dialog/ (приватный по умолчанию)
```

---

### Ключевой сценарий 3: Просмотр командой

```
1. Разработчик A делает диалог публичным через UI
2. Коммитит и пушит в репозиторий
3. Разработчик B делает git pull
4. Запускает npm run dialog:ui
5. Видит диалоги разработчика A
6. Изучает подходы к решению задач
```

---

## 🛠️ Technology Stack

### Frontend Framework
**Selected:** Vanilla HTML/CSS/JavaScript (статический UI в `public/`)

### Language
**Selected:** TypeScript 5.3+

### Backend
**Selected:** Node.js 18+ с Express 4.18

### Dependencies
```json
{
  "chokidar": "^3.5.3 - File watching",
  "express": "^4.18.2 - Web server for UI"
}
```

### Build Tool
**Selected:** TypeScript Compiler (tsc)

---

## ✨ Core Features (MVP)

### Уникальные функции

1. **JSONL → Markdown конвертер** — Парсинг Claude Code сессий
2. **Git visibility control** — Управление через .gitignore
3. **Watch mode** — Автоматический экспорт новых сессий
4. **Web UI** — Просмотр и управление диалогами
5. **Auto-summary** — Генерация саммари через Claude CLI
6. **Author attribution** — Атрибуция из git config

### Стандартные функции

- Express для веб-сервера
- Chokidar для file watching
- Нет БД — файловая система как хранилище

---

## 📊 Data Structure

### Main Entities

```
Session (from ~/.claude/projects/)
  - id: string (UUID)
  - filename: string (*.jsonl)
  - projectPath: string
  - messages: Message[]
  - summaries: string[]

ExportedDialog (in .dialog/)
  - filename: string (YYYY-MM-DD_session-*.md)
  - author: { name, email }
  - isPublic: boolean (via .gitignore)
  - summary: string | null
  - content: Markdown
```

### Relationships

- One Project → Many Sessions
- One Session → One Exported Dialog
- Visibility controlled via .gitignore entries

---

## 🔐 Security & Compliance

### Security Requirements

- ✅ Локальная обработка — данные не отправляются на сервера
- ✅ Приватность по умолчанию — все диалоги в .gitignore
- ✅ Контроль пользователя — явное переключение видимости
- ⚠️ Чтение файловой системы — доступ к ~/.claude/

### Data Privacy

- Данные хранятся локально
- Нет телеметрии
- Нет внешних API (кроме опционального Claude CLI для саммари)

---

## 📈 Scale & Performance

### Expected Scale

- ✅ < 100 пользователей (CLI утилита)
- Типично: 10-100 диалогов на проект
- Размер диалога: 10KB - 5MB

### Performance Requirements

- Экспорт одного диалога: < 100ms
- Watch debounce: 2 секунды
- UI загрузка: < 500ms

---

## 🔄 Development Approach

### Модульная структура

**Selected:** Модульная (recommended)

**Модули:**
- `cli.ts` — CLI интерфейс
- `exporter.ts` — Логика конвертации JSONL → MD
- `server.ts` — Express сервер для UI
- `watcher.ts` — Chokidar file watcher
- `gitignore.ts` — Управление .gitignore

### Development Style

**Selected:** Iterative — feature by feature

---

## 🎯 Success Criteria

### MVP Definition of Done

- [x] Экспорт сессий в Markdown
- [x] Сохранение в .dialog/
- [x] Управление через .gitignore
- [x] Watch режим для автоэкспорта
- [x] Web UI для управления
- [x] Author attribution
- [x] CLI команды (init, watch, ui, export, list)
- [x] npm scripts в package.json
- [x] Документация (README.md)

### Post-MVP Plans

- [ ] Поиск по содержимому диалогов
- [ ] Фильтрация по дате/автору
- [ ] Экспорт в другие форматы (JSON, HTML)
- [ ] Статистика использования Claude Code
- [ ] Интеграция с другими AI assistants

---

## 📝 Additional Notes

### Особенности проекта

1. **Self-contained** — утилита работает в подпапке .claude-export/
2. **Zero-config** — install.sh настраивает всё автоматически
3. **Non-invasive** — не модифицирует код проекта, только добавляет .dialog/

### Ограничения

- Только для macOS/Linux (пути в формате Unix)
- Требует Node.js 18+
- Claude CLI нужен для auto-summary (опционально)

---

*Filled: 2025-12-05*
*Version: 2.1.0*
